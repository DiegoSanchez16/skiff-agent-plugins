---
description: Fix a Skiff ticket from the current local repo using Skiff MCP.
---

# Fix Skiff Ticket

Ticket: `$ARGUMENTS`

Use the Skiff MCP server named `skiff`. Read the full ticket, ticket assets, and fix packet before editing code. Confirm the linked repo matches the local repo, propose a short plan, implement the fix, and summarize verification.

