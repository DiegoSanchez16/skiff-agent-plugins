{
  "mcpServers": {
    "skiff": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://app.getskiff.com/api/mcp",
        "--header",
        "Authorization:Bearer ${SKIFF_MCP_TOKEN}"
      ]
    }
  }
}
